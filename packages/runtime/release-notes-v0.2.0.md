# EvoZeus Runtime v0.2.0

This release isolates Stable and UAT runtime data and adds the CoEvolve external-sidecar runtime slice.

EvoZeus supplies the active channel state directory and exact Session Signal component root. Direct runtime users can provide the same inputs with `EVOZEUS_RUNTIME_STATE_ROOT` and `EVOZEUS_OFFICIAL_REPO_ROOT`.
