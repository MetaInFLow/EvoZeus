# Project Insights Report Template

This reference template renders EvoZeus project-level session insight reports.

The renderer consumes already-redacted ledger/report data and writes a static local-first HTML report. It does not rescan source sessions, upload raw content, or call external services.

Required output assets are copied to the report directory under `assets/` when HTML rendering runs.
